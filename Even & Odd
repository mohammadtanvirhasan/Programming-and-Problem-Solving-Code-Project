#include <stdio.h>
int main()
{
   int n, m, a = 0, b = 0 ;
   scanf("%d", &n) ;
   for(int i = 0;i < n;i++){
      scanf("%d", &m) ;
   if(m % 2 == 0)
      a = a + m ;
   else if(m % 2 != 0)
      b = b + m ;
   }
   printf("%d %d", a, b) ;
   return 0;

}
