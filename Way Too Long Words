#include <stdio.h>
#include <string.h>
int main()
{
    int n ;
    scanf("%d", &n) ;
    char s[101] ;
    for(int i = 0;i < n;i++)
    {
       getchar() ;
       scanf("%s", s) ;
       if(strlen(s) > 10)
       {
          printf("%c%d%c", s[0],(int)strlen(s) - 2, s[strlen(s) - 1]) ;
       }
       else{
          printf("%s", s) ;
       }
       printf("\n") ;
    }
 
 
    return 0;
}
