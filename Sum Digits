#include <stdio.h>
int main(){
int n;
scanf("%d",&n) ; //Read the number of digits
char digits[n + 1] ; // +1 for null terminator
scanf("%s", digits) ; //Read the digits as a string
int sum = 0 ;
for(int i = 0;i < n;i++){
   sum += digits[i] - '0' ;
}
printf("%d\n", sum) ;
return 0 ;
}
